# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  secret_key_base: 39e3bb630de95eda990f3792d9017bcdcdefd1dccd85fbe166d1f0cb29aceae9111e48999aa5115321db098d91886d1a89a7f0c01a5b6c80e88b17f6fe9a7522

test:
  secret_key_base: 305345595ec6dfb4cc3534742c2cf28b91fe488da5d5c343a9f2b278dc6e349590b693858dd87c3e0d49b98e021d0a5688580f69be641dd9629a35d9dbde7f53

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.
# <%= ENV["SECRET_KEY_BASE"] %>

production:
  secret_key_base: 123456789
